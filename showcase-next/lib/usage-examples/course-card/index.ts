import { courseCardInProgressExample } from "./01-in-progress";
import { courseCardNotStartedExample } from "./02-not-started";
import { courseCardCompletedThumbnailExample } from "./03-completed-thumbnail";
import { courseCardCustomIconExample } from "./04-custom-icon";

/** One file per example; each string is self-contained (imports + single usage). */
export const courseCardUsageExamples = [
  courseCardInProgressExample,
  courseCardNotStartedExample,
  courseCardCompletedThumbnailExample,
  courseCardCustomIconExample,
];
